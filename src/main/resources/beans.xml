<?xml version="1.0" encoding="UTF-8"?>
<beans 
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
						http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
					    ">

	<context:property-placeholder location="classpath:application.properties"/>

	<!-- <bean
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location" value="classpath:config.properties" />
	</bean> -->
	
	<bean
		id="settings"
		class="ca.empowered.nms.simulator.config.Settings">
		<property name="hostCount" value="${eni.nms.simulator.count.Host}" />
		<property name="webApplicationCount" value="${eni.nms.simulator.count.WebApplication}" />
		<property name="evenlyDistributeRelationships" value="${eni.nms.simulator.relationships.distribution.even}" />
		<property name="randomizeEventGeneration" value="${eni.nms.simulator.events.randomize}" />
		<property name="nodeNameSuffix" value="${eni.nms.simulator.node.name.suffix}" />
	</bean>
	
	<bean
		id="nodeFactory"
		class="ca.empowered.nms.simulator.api.NodeFactory">	
	</bean>

</beans>